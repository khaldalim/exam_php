{% extends 'base.html.twig' %}

{% block title %}Stagiaire{% endblock %}

{% block body %}
    <h1>Stagiaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stagiaire.id }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ stagiaire.createdAt ? stagiaire.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stagiaire.name }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ stagiaire.phone }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ stagiaire.birthday ? stagiaire.birthday|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('stagiaire_index') }}">back to list</a>

    <a href="{{ path('stagiaire_edit', {'id': stagiaire.id}) }}">edit</a>

    {{ include('Admin/stagiaire/_delete_form.html.twig') }}
{% endblock %}
